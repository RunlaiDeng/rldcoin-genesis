# Rldcoin v5.0 总规划：面向未来人类的星际点对点转账系统

> 星际转账目标对齐版 · 2026-09-14。本文是唯一执行计划，沿用 v5.0 的协议规则与既有阶段、规范和门编号。具体编码与经济规则见第18节，实现进展与验收索引记入 [PLAN_STATUS.md](PLAN_STATUS.md)。正式创世、生产密钥、真实价值和对外发布按相应阶段处理，不能由修改文档或状态字段代替。

## 0. 目标与执行范围

**Rldcoin 的主目标是成为用于未来人类星际间的点对点转账系统，让地球、太空居住地、飞船与遥远恒星系的聚居地之间能够自主、安全地转移同一种 RLD 资产。**

项目面向长期运行的正式网络。从地球启动是实现该目标的第一步；测试网、开发网络、模型与仿真是实现过程中的验证工具。产品的价值在于跨距离、跨断联周期的所有权转移与可验证结算，而不只是在一台机器或一个地球网络内完成付款。

### 0.1 最终要交付的用户能力

1. 发送者控制自己的密钥，确认收款人、真实目标 Zone、费用上限及跨域条件后签署付款。
2. 各区域以本地共识处理本地事务。远方失联时，只要本地安全条件满足，已有合法本地余额仍可使用。
3. 跨区域付款沿可验证的源锁定、证明传递、目标导入与异步回执完成；目标余额可用后，不因回执迟到被源端重新释放。
4. 钱包准确展示付款所处阶段、缺少的信息和可执行动作。信息尚未送达时不显示已到账，也不承诺无法保证的到达时间。
5. 独立接收方能依据自己认可的信任策略、源历史与证明核验付款，无需联系发送者的账户服务或始终在线的地球清算中心。
6. 长期离线、节点故障、介质更换或算法迁移后，能够在既有授权和可信证据范围内恢复身份、资产历史和未结转账。

上述能力的实现完成度由第16节的可复现实验与实际部署证据判断，不能只凭功能名称、接口存在或模型通过认定。

### 0.2 架构与开发主线

采用 **区域内共识、区域间异步证明交付**。每个 Zone 维护本地账本、共识与恢复机制；星际通信不进入所有本地付款都必须等待的全局共识回路。不同 Zone 的高度、时钟与 Era 分别解释，通过带所属域的认证引用建立联系。

地球节点、公开参与和验证权交接为长期运行建立基础；跨 Zone 转账、长延迟、断联、介质携带和恢复验证从早期同步推进。每个会影响跨域身份、资产状态、费用、签名或恢复的改动，都必须检查其在长期消息延迟下是否仍然成立。

开发按第14节的依赖推进。跨域研究与零价值验证可在 P0–P5 期间完成相关部分；真实价值和具体路线各自满足条件后启用。不得因永久仪式或外部人员暂缺就停止可独立执行的协议实现与验证。

### 0.3 范围与完成边界

- 核心范围：非托管付款、区域独立运行、固定供应与迁移守恒、异步交付、长期断联、身份与算法连续性、恢复、开放参与和合法升级。
- 配套范围：节点与钱包、转账状态展示、可携带证明包、中继与归档、运行监测、可复现发行和逐条路线验收。新增服务与经济机制必须说明如何支持这些能力。
- 不纳入当前范围：建设航天器或星际通信设施、超光速/虫洞与时间环、交易所推广、没有真实需求的完整科研市场、无用途的“全宇宙”规模指标。
- 验证规模由目标负载、延迟、证明大小、保存期限和资源预算决定；不承诺任意年限的永久在线或无条件恢复。
- 软件在地球环境中的工程验收与真实远方部署分别记录。逻辑时间跳跃不等于多年运行，地面仿真不等于真实星际网络；现实通信条件具备后再验收相应部署。

“主网”分两个时点：M0 公开永久网络身份，初期 `VALUE_CAP_0`，不可转让 RLD；M5 及价值安全门通过后，在同一历史中启用受限支付。永久网络的早期零价值阶段仍服务于正式系统的建设。实际完成项与未完成项只按 [执行状态](PLAN_STATUS.md) 及其精确版本证据认定。

## 1. 不可变规则

1. 名称Rldcoin，代码RLD，最小单位runlai；`1 RLD = 10²⁴ runlai`，固定总量1000亿RLD，即 `10³⁵ runlai`。
2. 无创始人/投资人可自由出售预分配；全部RLD从协议储备按真实服务规则进入流通，费用与奖励不增发、不销毁。
3. 运行观察/全节点无需RLD、官方账号、邀请或许可。持币、质押、节点数、公钥数和在线时长不增加验证权。
4. 验证资格来自可复验、不可重复、成熟且未过期的贡献票；服务报酬与共识资格分别计算。
5. 创始特殊权重只能下降，不能恢复。多台创始服务器、不同密钥或AI工具仍属于同一控制组。
6. 用户私钥由用户控制；不存在管理员改余额、重置历史、恢复创始席位或没收普通钱包余额的后门。
7. 金额持久化使用U128，乘法/汇总使用检查溢出的U256；JSON金额为十进制字符串，禁止浮点、截断、饱和与回绕。
8. 协议高度、Epoch/Era与长期序列采用U128目标格式；现有U64路径须显式迁移，不能仅改显示字段。每个Zone计数独立，不用跨Zone裸高度比较因果先后。
9. 共识/签名使用版本化规范字节，绑定网络、货币创世、Zone、Era、对象版本和用途；未知、非规范、重复或越界输入拒绝。
10. 所有状态变更、费用、Nullifier、成员、签名锁和恢复都遵守同一权威链与失败原子性；失败不能留下半笔付款或半次换届。

## 2. 单人启动与同链升级

### 2.1 启动包

首个网络只有Earth Zone。沿用现有固定4验证进程、隔离签名器、观察进程和见证服务的安全语义，统一包装安装、启动、状态、停止、备份与恢复。进程数量不作为独立运营者数量。

- 发布可复现源码/二进制、校验清单、创世文件、安装/卸载和恢复说明。
- 创世pin来自运行机和备份之外；每个节点独立核验。临时E2E的创世/密钥/报告不能成为官方身份。
- 钱包、验证、见证、发布和传输密钥分离；签名/管理端口不对公众开放。
- 同一共识密钥同时只有一个可释放签名的活动实例。恢复先隔离旧实例，再核验外部单调锚、状态根与投票锁。
- 正式 M0 可按第2.3节采用单控制组的远程启动配置：真实资源基准、受限软件签名、跨机认证传输及经过恢复验证的两机加密备份。其硬件保管、控制者独立性和物理离线恢复限制必须公开。细则见 [M0运行手册](operations/M0_MAINNET.md)。
- 分角色公开CPU、峰值内存、同步量、存储增长、带宽与恢复时间。满盘前预警并安全暂停，不删除未获授权裁剪的证据。

### 2.2 永久创世前的升级出口

M0a 的默认实时签名锁定心跳模式。永久仪式前，必须对拟发布的精确版本验证显式局部授权下的首次升级安排、激活和角色恢复，证明网络能合法演进。首次迁移验收不自动覆盖后续任意迁移，也不能通过删除 guard 绕过升级授权：

1. 创世绑定阶段转换、升级授权、供应与身份不变量、创始退出约束。出生事实只读，活动能力由后继状态表达。
2. 升级包含规范/代码/迁移哈希、前后状态根、激活高度和不可逆点，由当时合法委员会最终化。安装器、网页和创始钱包不能替代共识授权。
3. 节点、签名器、见证器、WAL、检查点和追赶共同迁移；供应、历史、准入头、票/claim和未结义务连续。
4. 新实现从原始创世重放或验证已审计连续性链；旧实现遇到不支持的激活必须停止。混合版本、崩溃和重启不能产生两个合法最终历史。
5. M0a→M0b及开放贡献激活仍为 `VALUE_CAP_0`。不得复用冻结tag作为任意升级指令，也不得改出生清单伪装原规则。

完整进程网络、混合版本及故障验收必须绑定源码和构建，已完成范围见 [PLAN_STATUS.md](PLAN_STATUS.md)。新改动只复核受影响的边界，不把旧报告自动扩展到新版本。V2 候选不可借配置转换为 V3；正式创世后不得偷换网络身份，没有合法恢复授权时宁可停机。

### 2.3 可远程完成的永久创世配置

用户于 2026-09-22 明确授权修改 P1 验收条件，并由代理远程完成正式创世及上线。采用 `P1_REMOTE_ZERO_VALUE_V1`，仅改变本阶段的运维与审查准入条件，不改变固定总供应、签名授权、状态守恒或后续价值门：

- **技术复核**：允许代理完成有精确源码、实测输入、反例与可复验记录的技术审查。记录审查者与实际范围，`independent_review_complete` 保持 false。独立安全审查移至开放有奖励的参与路径和真实价值启用之前，不把代理自查称为外部审计。
- **密钥保管**：允许既有明确到期的软件密钥例外，截止 `2026-10-07T16:00:00Z`；到期停止签名，不自动续期。运行、发布与个人收币密钥分离，个人收币私钥仅保存在用户本机。此配置不声明硬件签名或非导出性。
- **备份和恢复**：采用线上运行主机与本机的分离存储、加密归档、完整清单和实际恢复核对，替代 P1 的实体离线封存与人工见证要求。恢复先停止原签名实例；同时记录两台机器仍归一人控制。硬件保护、独立保管和物理离线恢复继续属于后续安全资格要求。
- **通信与公开入口**：同一受控主机内的角色服务仅监听回环地址，公众不能调用签名和管理接口；跨机操作使用固定主机身份的认证传输。公开数据下载和观察接口使用 HTTPS、只读路由和资源限制。公开观察不等于允许提交工作或获得发行奖励。
- **发布和持续身份**：生成全新正式密钥/创世输入，公开创世、源码/构建、发行规则、控制关系、校验方法、状态渠道和外部哈希存证。以实际恢复、同一身份重启及观察同步结果支持签署的永久生日声明。安装工件生成时的候选标记是历史事实；正式指定通过另外的签名声明完成，不覆盖旧工件或假称历史演练为正式仪式。

P1 只有在上述实际部署、恢复和公开核验全部通过后才能完成。旧的独立审查、离线保管前置条件由本配置在 P1 范围内明确替代；P2 的真实外部操作者、P3 的奖励/贡献闭环、P4 的交权及 P5/P6 的价值与路线条件保持有效。没有经过验证的发行机制，不得以运维脚本或创始签名直接发币。

用户随后明确要求：**创世不直接预分配 5%；按现有机制赚取至少固定总量的 5%，即 50 亿 RLD，再进行推广。** 这是推广前的经营目标，不是协议保证、保留份额或地址特权。用户已选择沿用现有发行机制：真实协议服务获得报酬，准入工作量证明本身不发币。新钱包仅接收真实验证通过的报酬。M0 心跳阶段不启用奖励，因此 P1 上线不等于已经开矿；后续奖励路径须通过既有阶段和价值门，不能把准入哈希数量或一份承诺记录显示为已挖到的余额。达到实际赚取目标前不开展推广。

## 3. 基础支付

同Zone付款：收款人签署一次性请求，付款人本地签名，节点验证所有权/余额/费用后经本地共识原子消费输入、创建收款及找零对象。

签名至少绑定收款OwnerId、金额、网络/源目标Zone、请求ID、对象序列、Era、费用上限、资源上限和明确所属Zone的逻辑有效期。付款请求与花费Nullifier只能使用一次。

钱包显示实际收款人、金额、手续费、押金、来源与最终状态。只有账本最终化后才能显示到账；请求被接收、交易广播或中继回执不算到账。同Zonep95≤3秒保留为待实测目标，不当作现有生产保证。

`OwnerId/ControlPolicy`支持所有者授权的密钥轮换、阈值控制与预设恢复。旧算法退役时，仅使用预先承诺且仍可信的后继路径；缺失时冻结，不由管理员猜测所有者。

## 4. 外部节点与创始交权

外部人员自行完成：获取并校验软件/创世 → 用官方或第三方/手工peer同步 → 比较已认证检查点 → 自选已开放角色 → 提交贡献 → 等待成熟 → 按规则入选并运行自己的签名器。

M0a只开放观察验证；M0b技术门通过后开放贡献，首个真实外部节点完成规定流程才达到M1。不能要求先达到M1才开放首个任务。网页、官方DNS和种子消失后，应能经已知peer或独立目录继续连接。

| 阶段 | 创始特殊权重 | 外部权重 | 外部控制组最低数 | 独立客户端最低数 | 核心验收 |
|---|---:|---:|---:|---:|---|
| M0 网络出生 | 100% | 0% | 0 | 1 | 同一创世、单控制组披露、可恢复、价值关闭 |
| M1 公开加入 | 100% | 0% | 1 | 1 | 陌生零余额节点独立同步并完成贡献路径 |
| M2 首次交接 | 75% | 25% | 4 | 2 | 外部真实票进入委员会；创始离线安全停机 |
| M3 共同控制 | 50% | 50% | 7 | 2 | 创始无法单独最终化；分区/恢复不分叉 |
| M4 外部法定数 | 25% | 75% | 11 | 2 | 创始全离线仍完成完整责任窗口的最终化/恢复 |
| M5 创始退出 | 0% | 100% | 21 | 3 | 特殊席位不可恢复；多周期与灾备通过，仍先零价值 |

每步由前序合法委员会最终化，不跳步、不回升、不按日期强行晋级。M2至少2个管理/网络故障域，M3至少3个，M4/M5至少3个地区或等价故障域；现实关联、同一管理员和共同不可替代服务须合并评估。

未来委员会为1000整数槽，法定数667；M0当前固定4成员需3票。单普通控制组最多100槽，单工作家族最多300槽；创始特殊槽另按表递减，M5后创始普通票也受100槽限制。人数只是最低门，不证明实际独立。已激活委员会不足法定数时停机，不回退为单节点出块。

M0–M3依赖创始活性。旧法定数拒绝交接或关键密钥全部丢失时，外部节点不能凭人数制造合法签名。控制关系的证据、挑战、争议和合并规则必须客观公开，不得变成创始白名单；没有可执行规则时不能宣称无许可验证者准入完成。

## 5. 资产守恒、快照与恢复

每个对象和跨Zone迁移只有一个规范金额位置：源导出后不可消费；目标导入后源在途条目只是待回执影子，不能重复计入供应。所有储备、费用池、押金、罚没池和未结托管按唯一对象/迁移边去重后，合计始终为 `10³⁵ runlai`。

### 5.1.1 有界快照与剪枝

剪枝前必须有已认证的干净切点、独立归档可用性、完整重建与外部反回滚证据；不能用同目录校验和代替外部信任。导入验证创世/可信检查点、Era链、QC、供应、状态、准入历史与有界后缀；验证完成并持久化后才原子切换新目录。

故障矩阵覆盖每个fsync/rename边界、尾部撕裂、旧快照/锚回滚、归档缺块、同代异根、未知算法和导入后首次签名。失败保留旧库。规范见 [有界连续性快照](spec/BOUNDED-CONTINUITY-SNAPSHOT-V1.md)。

## 6. 资源与抗垃圾交易

所有连接、请求体、列表、证明、sidecar、解码/验签/执行并发、缓存、热状态、WAL增长和同步批次均有硬上限。先做廉价长度/上下文/规范性检查，再验签、验工作和执行。

公众、peer、签名器、见证和管理通道分离资源预算；公众洪泛不能耗尽共识预算。收费不能代替限额，限额也不能代替费用。拒绝压缩炸弹、无界重试、任意URL拉取、路径穿越与来源伪造。

保留正常负载、目标峰值、超载和恶意流量测试；容量与预算据实发布，不要求毫无用途的万亿次真实请求。

## 7. 费用与状态成本

执行、证明、网络与长期状态分开计量。签名绑定定价Epoch、资源上限、付费对象、最高费用与有效期：

```text
resource_fee = Σ(actual_resource[i] × base_price[i])
total_fee = resource_fee + bounded_priority_fee + route_fee
          + destination_import_fee + notary_fee
          + optional_liquidity_fee + declared_risk_premium
```

状态押金可退，不冒充手续费。实际费用不得超过授权；余款按确定规则退回。失败费只用于已授权且纳入执行的发送者可控失败，无效签名、节点错误或未接纳请求不扣款。

内存池须有替换/驱逐、非零费用下限和区块资源上限。拥堵参数与费用分配先在测试网校准，变更延时生效；没有充分证据不增加经济功能。详细资源向量、计算顺序、押金、舍入及分配见 [账本与经济约束](spec/LEDGER-ECONOMICS-CONSTRAINTS.md)。

## 8. 共识与运行安全

- BFT安全依赖恶意权重严格少于1/3；投票在释放签名前持久锁定，认证视图切换不能用现有终局QC冒充可解锁HighQC。
- 任何成员/责任变化由唯一最终状态派生，Proposal/Prepare/Commit/Timeout/ViewChange、同步、签名器和见证必须使用同一成员解析器。
- 节点不持有生产验证私钥；签名器拒绝任意签名、跨域、旧头、弱法定数和持久化前签名。
- 外部回滚见证的成员、密钥、门限与最终头必须有可信策略/QC绑定；导入者自带三把钥匙不算外部法定数。
- 对peer身份、认证传输、DNS/种子替代、邻居多样性、Eclipse/分裂视图、持久同步进度与恢复做真实故障验证。
- 生产发行绑定源提交、可重现构建、SBOM、更新签名与反回滚。独立模型、同人多进程或仿真不冒充独立生产运营者。

## 9. 长期可恢复性

保留规范、可运行解码器、检查点、必要历史、控制策略、算法迁移链与恢复说明的多实现/多介质副本，定期实测重建。介质或算法更换必须保持资产谱系、供应和所有权连续。

旧签名超过信任窗不能独立证明新状态。算法突发失效而无迁移前锚点/后继承诺时冻结相关资产。恢复预算不足时缩减新服务并保留已承诺义务，不增发、不回收长期未动资产、不承诺永久在线。

## 10. 星际与跨Zone转账

本节定义 Rldcoin 的核心产品链路。跨域实现必须贯通节点、签名器、见证器、持久状态、交付通道和钱包，不能停留在证明格式或单个导入接口。

### 10.1 区域独立与源信任

每个 Zone 按本地延迟完成共识，维护自己的高度、Era、委员会与恢复。Earth 不是永久中心清算服务器；新 Zone 零 RLD 起步，不能靠复制创世根或磁盘生成资产。当地共识需要满足自身法定数和安全条件；“不等待远方”不代表本地失去合法共识后仍可继续付款。

目标分别验证源最终性、执行合法性、数据可用性、资产谱系、Nullifier 和风险预算。验证采用目标认可的源身份、委员会/算法连续性和信任策略，不能把发送者自带的密钥名单直接当作可信来源。证明不足、源冲突、信任窗过期或未知 Era 时观察/隔离，不创建可消费 RLD。

源目标之间不存在统一实时高度，也不承诺即时全局供应快照。跨区转账只要求相应参与方获得必要证明，不要求宇宙所有节点同步获知。

### 10.2 唯一资产迁移与异步回执

```text
发送者签名 → 源Zone最终锁定 → 目的地绑定付款证明
→ DTN/中继/携带介质 → 目标验真并最终导入 → 收款人可消费
→ 目标最终回执异步返回 → 源关闭影子记录
```

- 源锁定与目标导入共享同一经济迁移标识和去重规则；分包、重传、更换路线或介质不能产生新的可花费副本。
- 目标已导入而回执丢失时，目标余额保持合法；源保留待闭合记录，不能据此重新释放原资产。
- 退款必须先验证目标最终化的永久拒绝，该拒绝消费与导入共用的 Transit Nullifier。单纯超时、未包含证明或胶囊被带回均不够。
- 同 Zone 付款不等待远方确认。断联时既有合法本地余额可按本地安全条件继续使用，新跨域付款可能等待。
- Zone 分裂须有最终化、无重叠的资产分区清单；未解决的竞争分支不能同时被当作合法来源。

### 10.3 长延迟交付与携带介质

交付通道传递证明，资产可消费性由账本规则决定。实现必须支持可重发、可校验、可断点恢复的有界证明包，并约束每个来源的队列、带宽、验证工作、归档空间和重试预算。

DTN 的缓存 TTL、消息到期与上传超时只清理传输资源，不退款、不改变所有权。运输副本按策略过期前，必须确认必要证明仍有可恢复来源；空间不足时暂停接收新义务，不擅自删除唯一结算或恢复证据。

旅行者胶囊出发前绑定唯一真实目标 Zone，到达后仍须满足目标信任、算法和可用性政策。飞船可先建立 Starship Zone 并迁入资产，不能预先绑定不存在且可随意更名的目的地。跨算法窗口的长期航行须明确携带哪些连续性材料；材料失效且没有合法恢复路径时保持隔离。

路线切换只能使用付款人已授权的费用与目的地范围。中继失联、介质损坏或长期保存成本变化不能静默扩大付款人的扣款授权。

### 10.4 钱包与接收体验

钱包分别显示源已锁定、证明待交付/在途、目标已可消费、回执已返回、已证明拒绝及隔离。状态来自可验证账本或证明，不把中继接收确认当作到账。`UNIVERSALLY_SETTLED` 仅表示本次迁移闭合，不表示所有节点实时知晓。

发送前展示真实目标、收款人、金额、费用上限及影响完成的条件；发送后提供查看证据、在预算内重传、导入回执和恢复记录的入口。缺少返回通道时允许导出回执等待携带，不要求收款人持续连接地球网页或发送者服务。

远距离付款不显示无依据的即时到账承诺。相距 4 光年时，仅信息传播就有约 4 年单程下界；本地快速处理不能消除传播时间。

### 10.5 路线启用与验收

跨域暴露按来源、关联控制、Zone、路线、Gateway、流动性与总额封顶。需要本地担保/保险的模式不能把源方自报抵押当作可追索资产。每条路线独立启用，地球支付开通不自动打开跨域风险。

DSC 是以后可选能力：用目标已存在的 RLD 垫付；本地付款、唯一清算索取权与 Nullifier 原子完成，后台失败不得追回收款人已到账资产。首版本地付款不依赖 DSC。

第16节 I-01–I-14 是跨域工程验收主线：包括消息时序、资产唯一性、地球失联下的区域运行、实际进程交付、持久队列恢复、跨算法/身份连续性和钱包证据。每个场景分别记录模型、实际进程、独立操作者和物理通信证据，按已通过层级描述能力。

## 11. 开放贡献与经济启动

零余额节点可完成：公开参加承诺 → 未来挑战 → 有界结果/证明 → 确认前缀 → 客观复验与去重 → 贡献票成熟 → 委员会选择。

无资产 `PermissionlessAdmissionLog`以累计有效工作排序，限制垃圾并记录参与/结果；不承载余额或发RLD。主账本采用确认前缀；持续遗漏合法准入须进入 `CENSORSHIP_STALLED`并有认证恢复路径。不得先启用不可恢复的停签逻辑。

挑战随机性不能由创始、求解者或最后提交者重抽；单人可操纵的历史/种子不证明公平。信标不满足假设时暂停相关发票/抽样，不能事后把实验记录洗成正式资格。

首发任务围绕复算、供应审计、数据持有/恢复、可用性和密码连续性。`BaselineAccessWork`、注册、在线、自报资源、重复结果和主观科研评价不发贡献票。

每工作家族最多300槽，因此M5至少需要4个真正可用的家族。三个家族最多900槽，增加操作者不能补齐。第四家族候选是有真实需求且可防合谋复用的网络交付；M4后新增须按规范审核、独立实现和挑战期验证。证明不了就不推进M5，不抬上限凑票。

创世储备固定分池：1%启动服务、9%连续性/归档/迁移、90%真实需求匹配。储备无私人提取钥匙；任务先锁精确预算，按真实交付、挑战、去重和每Epoch/Era上限结算。

`StarterServiceLane`按预算与能力显示 `TEST_CREDIT_ONLY / WAITLISTED / PAUSED_NO_BUDGET / OPEN`。零价值阶段只能有贡献资格及事前预算锁定的不可转让 `DeferredServiceClaim`；不保证兑换。价值启用后仍须核验原预算、证据、到期与Nullifier才可结算。没有需求/预算不承诺收入。

手续费进入费用池后聚合领取；普通持币与服务保证金不买票。需求池不能仅凭自生成手续费自动匹配；关联请求者/服务者/中继/验证者串谋回收费用后不得获得正净补贴，最坏储备损失须硬封顶。

## 12. 升级与治理

每次升级绑定规范、实现、迁移、向量和激活条件。价值阶段要求本地严格超过2/3验证权及冻结规则下独立公证法定数；创始零价值阶段仅按第2.2节有限契约演进。

算法/哈希轮换采用已验证的旧新连续性证书、所有者后继授权与明确迁移窗口。不同Zone自行接受兼容升级，未知算法停止导入，不静默降级。

紧急动作仅能降低新风险、隔离来源或暂停；不能增发、没收、回滚最终付款或恢复创始权。下调 `VALUE_CAP_0`不消灭已有义务，只允许按原承诺关闭既有风险而不扩大金额/受益人/用途。

## 13. 产品交付

- **节点包**：安装、身份校验、同步、资源上限、角色选择、运行状态、升级、备份/恢复与退出；关闭网页后节点继续运行。区域运行不依赖一个永远在线的地球服务。
- **非托管钱包**：本地密钥/硬件签名、收款请求、目标 Zone、明确费用和可验证状态；跨域付款贯通发送、交付、目标到账、回执与拒绝退回。生产私钥不进浏览器 localStorage、日志或遥测，开发钱包不能直接升格生产。
- **跨域交付与恢复包**：生成、核验、持久保存和导入有界证明/回执；支持中继及携带介质，暴露必要的连续性材料、缺失依赖和资源预算。界面不把“导出证明”显示为“转账完成”。
- **观察/节点中心**：显示创世、最终头、同步来源、控制组、阶段、任务、实际成本、贡献和待闭合转账。观察节点不伪装验证者，消息失联不伪装资产丢失或退款。
- **公开说明与发行**：提供源码/构建校验、非官方 peer 接入、恢复说明和故障渠道。README 用英文阐明星际点对点转账定位；当前能力按精确验收证据描述，不承诺收益或将未完成阶段写成已部署。

每项交付都必须说明支持的网络阶段、所需证据和实际可执行的用户路径。测试网资产、模拟证明与临时身份只用于开发验证，不作为正式网络身份或资产凭据。

## 14. 唯一执行顺序

P0–P6 保留既有阶段含义和编号，分别约束交付依赖与启用条件。地球启动为星际系统建立可信基础；异步跨域工程在依赖满足时同步推进，不等到地球支付上线后才开始设计。

| 阶段 | 要完成的交付 | 退出条件 |
|---|---|---|
| P0 候选收口 | 精确源码基线；相关测试；单人安装/备份/恢复；真实资源基准；第2.2节升级出口；明确冻结缺口 | 可复现、可恢复、同链可演进；仍是候选 |
| P1 永久网络生日 | 第2.3节远程启动配置、已明确且已实现的创世发行规则、正式运行与加密恢复、真实创世输入、外部 pin、公开工件与声明 | 实际上线、同一身份恢复及公开核验通过；永久声明完成，转账价值门保持关闭 |
| P2 外部观察 | 外部操作者在自己设备安装，同步、替换 peer、重启追赶 | 不使用创始密钥/账号的异机证据；仍不等于 M1 |
| P3 开放贡献 | 完整 tag28 传输/独立语义/持久授权、准入与审查恢复、贡献/票/预算闭环 | 零余额陌生节点完成 M1；不能以上传 proof 成功替代 |
| P4 动态交权 | 信标、唯一 Epoch、全路径成员切换与责任、足量工作家族、M2–M5 | 各阶段操作者/客户端/故障域与离线演练通过 |
| P5 地球受限支付 | M5 及第17节全部适用价值门，完整钱包、费用、监测与恢复 | 精确安全论证通过，才启用本地 `VALUE_CAP_1` |
| P6 星际转账工程与逐路线交付 | 多 Zone 异步转账；长延迟/断联交付；飞船/携带介质；钱包回执与拒绝退回；长期恢复；来源与路线审查 | 第16节跨域矩阵在声明的实现/部署范围内通过；真实价值另满足适用价值门与每条路线的启用条件 |

### 14.1 跨域工程的持续推进

P6 按下列依赖逐步交付，各部分保留独立的范围与证据：

1. **状态与证明闭环。** 从 P0 起核对现有代码，先完成零价值的源锁定、目标导入、异步回执、拒绝退回与唯一资产位置验证。对消息提前到达、乱序、重放和永久断联给出可执行负向场景。
2. **实际进程与传输恢复。** 随节点、签名/见证及同步路径成熟，贯通多个实际 Zone 进程、持久交付队列、带宽/存储限制、携带介质和钱包；验证地球入口关闭后已获合法授权的区域仍能在本地安全条件下运行。
3. **独立运行与逐路线启用。** 在外部运营和安全条件满足后，完成独立操作者、真实网络故障与恢复演练。价值路线遵守 P5、第17节和该路线全部条件；未审路线保持关闭。

三部分是 P6 的工作分解，不新增绕过 P0–P5 的价值通道。地球环境可完成相应软件工程验收；真实太空或星际路线须待实际通信、设备与运营条件具备后另行验收和声明，不能因模拟多年延迟就写成已完成物理部署。

P2 外部操作与上述零价值工程可穿插。P0 的同链升级若依赖 P3 组件，先实现必要依赖并在未激活状态验证，不把阶段表当作禁止工作的循环。已有阶段完成后继续使用其精确验收基线，变更只重验受影响边界。

### 14.2 工作项选择与执行

优先级依次是：已发现的资产/签名/恢复安全缺陷 → 最早未完成且可执行的阶段依赖 → 第16节尚未贯通的跨域用户链路。新增工作须说明如何支持当地自治、异步转账、开放运行或长期恢复；外部依赖暂缺时转向可独立执行的工作，不虚构该依赖已经完成。

对每个工作项执行：**读现状 → 冻结必要差异 → 最小完整实现 → 相关负向/故障与端到端验证 → 记录证据 → 下一个未完成项。** 不重复重建已通过的模型；不只添加 helper/测试就宣称运行时已完成；不擅改供应、权重、费用守恒或永久编码来让测试通过。

用户已授权本地设计细化、代码修改、构建、必要测试、文档状态更新与可逆实验。需要用户实际设备、生产签名、外部人员/机构、公开发布或价值启用时，先完成可独立做的部分和具体可审查工件，再准确列出缺失条件。已有授权在其范围内继续有效；未获明确授权的生产秘密不得代持或写入记录。

2026-09-22 的明确授权另覆盖第2.3节规定的远程正式创世、所需运行密钥、公开创世工件及上线操作，以及在本机新建由用户持有的收币钱包。该授权不包含开启真实价值转账，不包含伪造挖矿结果或直接划拨 5% 余额。发行方式沿用现有协议服务报酬规则，P1 不宣称奖励路径已启用。

## 15. 当前起点与首个任务

每次执行先读取 [PLAN_STATUS.md](PLAN_STATUS.md) 及对应源码、构建和终态报告，确定最早未完成且可执行的项目。主计划不复制易过期的完成清单，不把已验收 P0 自动退回“从头建立候选”，也不把后续改动自动纳入旧验收。

针对地球启动，核对永久创世、签名模式、准入、角色授权、恢复和价值状态的实际边界；按依赖完成未闭合部分。M0b、动态委员会、多运营者独立性与真实价值不得因为改了状态标志就算完成。

针对星际主目标，按第10节与 I-01–I-14 核对当前跨 Zone 实现：区分模型、开发进程、正式角色路径和实际独立运行。选择最早缺失的完整转账/回执/恢复链路，明确所需源码、状态不变量、角色与失败情形，再实施验证。已有两 Zone 或飞船仿真应被复用，但不能当作正式异步交付链路已经闭合。

`PLAN_STATUS.md` 每项记录任务 ID、规范/源码/构建绑定、验证命令/结果/工件、已覆盖边界与剩余缺口。跨域项目另外注明通信模型、逻辑/实际时长、控制组及运行环境。测试数量和旧报告不是当前生产资格；已有未提交文件不得删除或重置，建立版本基线时须检查秘密与无关内容。

## 16. 验收清单

| 编号 | 必须验证的行为 |
|---|---|
| E-01 | 全新目录安装/创世候选、外部pin、精确重放、停止/启动、备份/恢复保持身份与状态；篡改失败关闭 |
| E-02 | 同链升级前后供应、历史、票/claim与签名锁连续；旧/新混合、每个持久边界中断不双最终 |
| E-03 | 真实外部设备无创始凭据同步/替换peer/恢复；官方入口失效后仍能参与 |
| E-04 | 无余额参加→随机挑战→有效结果→成熟票；重放、无需求、无预算、伪家族、预选种子和隐藏白名单失败 |
| E-05 | 1MiB proof及+1、48KiB块及+1、恶意计数、配额/TTL/GC/并发；最大合法对象贯通节点/签名器/见证器 |
| E-06 | 各角色独立验证准入历史/sidecar/最终锚/停签状态；非法或不完整证明不写锁、不签名、不提交 |
| E-07 | 审查停机与认证恢复、准入重组、target/rollover不回滚最终资产，不形成永久不可恢复死锁 |
| E-08 | 真实成员切换、票消费、责任与惩罚；M2/M3创始离线安全停机，M4/M5外部667槽持续最终化 |
| E-09 | BFT视图切换、隐藏旧法定数、领导者故障、分区重连、追赶、签名器/见证回滚与崩溃不双最终 |
| E-10 | 资源超载/洪泛/Eclipse/伪peer不绕过预算；拒绝路径不污染状态；公众流量不耗尽共识 |
| E-11 | 全部金额边界、费用/押金/claim/储备/迁移边去重守恒；串谋补贴、票购买、价值闸绕过失败 |
| E-12 | 快照切点、归档缺块/损坏/失联、旧锚、算法迁移、导入/剪枝中断与首次签名恢复 |
| E-13 | 源码/构建/发布绑定、钱包密钥与确认流程、独立客户端差分、真实运营与事故演练 |

星际与跨域核心验收矩阵（按第14.1节逐步贯通）：

| 编号 | 场景与预期 |
|---|---|
| I-01 | 普通延迟、分钟级和4年逻辑单程；本地共识独立，信息不提前到达 |
| I-02 | 黑障、乱序、重复、损坏和传输TTL到期；可重发、不重复导入、不自动退款 |
| I-03 | 目标已导入但回执丢失；目标余额保留、源退款拒绝，回执恢复只闭合一次 |
| I-04 | 目标永久拒绝后迟到胶囊；导入拒绝、源只退款一次 |
| I-05 | 改目的地/收款人、复制胶囊、新建或分叉Zone；不能建立第二个可消费位置 |
| I-06 | 源失联、旧委员会信任过期、算法退役、归档不足；新导入等待/隔离，不放行旧QC |
| I-07 | 两Zone同高度但因果不同、Era不同；只采用带所属域的认证引用 |
| I-08 | 飞船长期独立后重连或分裂；历史/供应连续，有无重叠分区清单 |
| I-09 | 地球/官方入口及远方服务关闭，本地仍有合法法定数与有效授权；当地付款与恢复独立，新的跨域导入按证据等待/隔离 |
| I-10 | 两个实际 Zone 的节点/签名器/见证和钱包贯通锁定→交付→导入→回执及拒绝退回；任何时点最多一个可消费位置，在途时可为零；金额按第5节唯一计入 |
| I-11 | 持久交付队列在部分写入、重启、满盘前限额和中继切换后恢复；不越费用/资源预算，不丢唯一未结证明，不重复结算 |
| I-12 | 携带介质导出、复制、损坏、重复导入和离线回执回带；身份/目标/签名严格核验，复制介质不复制可消费资产 |
| I-13 | 长期在途期间的委员会、算法与身份变化；连续性材料完整才按策略导入，缺失/过期不能以旧签名或管理员声明绕过 |
| I-14 | 钱包在各消息停点、回执丢失和重启恢复后显示可验证状态与实际费用；不提前报到账，不提供无证明退款，不将单次迁移闭合显示为全网实时知晓 |

模型、故障注入和逻辑时间跳跃只证明所覆盖性质；不得冒充真实断电、外部运营或多年运行。测试只按改动与未解风险选取，失败修复后重跑相关范围，不能无理由反复耗时全量循环。

## 17. 价值启用门

M0–M5均先为 `VALUE_CAP_0`。M5不自动开价值；必须另有绑定精确创世/源码/构建/风险上限的 `MainnetSafetyCase`、独立复核与公开挑战期，首开只能为 `VALUE_CAP_1`。风险容量延时上调、必要时下调；不回滚最终付款。

以下保留现有证据系统的24个门，完整 `GATE-17-*` ID不重编号、不删除，不能把示例 `INCOMPLETE/OPEN` 改为通过。对应映射见 `docs/spec/mainnet-safety-case.example.json`；示例不是生产证据。

| 原门号 | 必须关闭的风险 |
|---|---|
| 01–02 | 所有价值状态由多运营者BFT决定；开放贡献、独立控制、M5与创始退出完整 |
| 03 | 有界资源、内存池、定价和Eclipse/分裂视图防护 |
| 04–05 | 跨Zone最终性/有效性/可用性验证；迁移边与U128/U256供应守恒 |
| 06–07 | 所有权与密码生命周期；Zone身份连续和完整签名域 |
| 08 | 三个完整代码独立客户端；轻量模型不代替独立完整实现与真实团队证据 |
| 09–11 | 跨域/DSC风险预算、连续性资金/恢复、储备反合谋 |
| 12 | 有限升级/治理，不能改供应、没收资产或恢复创始权 |
| 13–14 | 外部审计/漏洞处理，可重现供应链、签名发行与更新保护 |
| 15–17 | 可重建恢复包、真实公开创世、可信源码历史 |
| 18–19 | 未过期安全论证、TCB清单、不变量与需求到证据追踪 |
| 20–21 | 共识强制价值限额、真实事故/密钥失陷/灾难恢复演练 |
| 22–23 | 适用法律与结算责任；无测试网权限/余额导入、无浏览器生产密钥 |
| 24 | 认证视图切换与带证书恢复，节点/签名器/见证持久化一致 |

禁用的后期能力仍需证明所有入口不可达，并评估对共享代码/资产的影响；不能删掉对应门绕过审查。跨域安全设计与地球环境故障验证不等于必须先建成物理星际网络。任何缩小发布能力范围都必须有明确规范与独立审查，不能仅把缺失证据标为“不适用”。

## 18. 规范、证据与执行约定

| 内容 | 唯一详细来源 |
|---|---|
| 开放贡献对象、schema/tag、哈希、准入链、委员会、错误顺序、硬上限与向量契约 | [开放贡献共识规范](spec/OPEN-CONTRIBUTION-CONSENSUS-V1.md)，保留原18.1–18.11编号 |
| 精确字节/签名/工作程序模型 | [Open Contribution Wire V1](spec/OPEN-CONTRIBUTION-WIRE-V1.md) 与其冻结向量 |
| 守恒、资源费、押金、跨域费用/风险、储备与结算 | [账本与经济约束](spec/LEDGER-ECONOMICS-CONSTRAINTS.md) |
| 快照/归档/反回滚/剪枝 | [Bounded Continuity Snapshot V1](spec/BOUNDED-CONTINUITY-SNAPSHOT-V1.md) |
| 共识证书与视图切换 | [线协议规范入口](../spec/wire/README.md) |
| M0运行与恢复 | [运行手册](operations/M0_MAINNET.md) |
| 实现现状与可复核证据 | [PLAN_STATUS.md](PLAN_STATUS.md) |

主计划决定做什么和何时允许启用；规范决定精确验证什么；状态文件记录实际完成什么。文档精简不改变既有schema/tag/哈希/向量/门ID。旧文档中的历史阶段、重复确认要求或任意远期规模要求若与本文冲突，以本文的执行范围和授权为准；真实安全发现与未通过证据仍然有效。

执行目标是“按 P0–P6 及其依赖，交付面向未来人类星际间的点对点转账系统；地球启动与异步跨域工程按依赖推进，真实外部条件逐项留证，未通过的启用门保持关闭”。先做最早未完成且可执行的项，完成一个完整用户链路再进入下一个。不得以再次重写规划、生成空模板、修改测试期望或只跑健康检查替代实现。

关键背景仅保留：[Bitcoin白皮书](https://bitcoin.org/bitcoin.pdf)、[DTN BPv7](https://www.rfc-editor.org/rfc/rfc9171.html)、[BPSec](https://www.rfc-editor.org/rfc/rfc9172.html)、[IBC异步数据包语义](https://github.com/cosmos/ibc/blob/main/spec/core/ics-004-channel-and-packet-semantics/README.md)。这些是设计参考，不证明RLD已具备其安全或互通能力。
